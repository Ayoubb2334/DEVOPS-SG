# ClusterIP : le frontend (Nginx) accède au backend via "http://backend:8080"
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: smartphone-app
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
